<h1>Artist Edit Form</h1>
<%= form_for @artist, method: "put" do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :bio %>
  <%= f.text_area :bio %>
  <%= f.submit "Update Artist" %>
<% end %>
