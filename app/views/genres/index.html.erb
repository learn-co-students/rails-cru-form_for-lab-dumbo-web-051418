<h2>Genres</h2>
<%= link_to "Create New Genre", new_genre_path %>
  <div>
    <ul>
      <% @genres.each do |genre| %>
      <li>
        <%= link_to genre.name, genre_path(genre) %>
      </li>
      <% end %>
    </ul>
  </div>
